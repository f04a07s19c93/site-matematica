function mdc_medio(x) {
    return "MDC( " + (Math.round(Math.random() * 500 + 50)) + " , " + (Math.round(Math.random() * 500 + 50)) + " , " +
            +(Math.round(Math.random() * 500)) + ") ="
}

module.exports = mdc_medio